（题目来源：牛客周赛122）

小苯的面前有一个长度为 n 的序列 a，他现在需要花费：a1​+a2​+a3​+...+an​ 的代价拿走 a 序列，但为了最小化代价，他可以提前做任意次以下操作：  
- 选择一个区间 l,r (1≦l≦r≦n)，花费区间最大值的代价，将区间中所有值均变为最小值。  
- 形式化的即，令：  
$$
mx=max(al,al+1​,al+2​,...,ar​),mn=min(al​,al+1​,al+2​,...,ar
​$$
接着花费 `mx` 的代价，对所有 `i` 执行 `ai​←mn,(l≦i≦r)`。  
你的任务就是求出小苯拿走 a 序列的最小花费。

首先，数组中的最大值必然包含在花费中。
介于这一点，我们可以分为两种情况：
- 不操作直接拿走数组中所有数字
- 操作
对于第一种情况显而易见，花费包含数组中的最大值。

对于第二种情况我们可以分为两种情况

- 操作的区间包含数组中的最大值

- 操作的区间不包含数组中的最大值

对于第一种情况，显然花费了数组中的最大值花费。

对于第二种情况，又回到了不操作的情况，显然也包括数组中的最大值。

综上所述，数组中的最大值一定会包含在花费中。



解决这个问题可以分为两种情况

- 不操作，直接花费总和sum

- 操作

通过上面的论述，显而易见操作一定要包含最大值，否则就没有实际意义了。那么既然已经包含最大值了，我们可以尽可能的让操作的范围大，换句话说，直接对整个数组操作。

最后，我们比较这两种情况的最终结果，输出这两种情况中的结果的较小的那一个即可。

代码如下

```cpp

```




